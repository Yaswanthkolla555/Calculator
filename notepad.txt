We use grid to simplify the css
we can use grid-template-clums and rows->for repeat(no.of col,size);

to select items just after-->a class we iuse > operator

when we use align content center -->sets all items intocter forminf calc

white color-->255,255,255,opacity
black-->0,0,0,;


to break word -->word wrap:break-word;
breka-word-.>breakall;